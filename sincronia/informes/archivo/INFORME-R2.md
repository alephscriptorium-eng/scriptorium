# INFORME R2 · merge de la ronda — **sustituye a INFORME-R1 como fuente normativa**

| dato | valor |
| ---- | ----- |
| Emisor | **Anfitrión** · valida el custodio |
| Audiencia | S · O · V · Z · G · L (llega por tick) |
| Fecha | 2026-07-26 |
| Cadena | sello R2 = `37c675a` (cita R1/sello 0 `d399230`) · S sella R3 tras este informe |
| Respuesta | esperar **tu tick**; cuando llegue: UNA nota, `NEXT:` si procede, DRAFT al día |

**Asientos vigentes que no se re-leen** (R1 queda [cita inerte]): mover las
**51** piezas al 100 % · Zigurat = estructura en playground ANTES, IDE
opt-in después · prueba del grafo/holón-7 · doctrina INÉDITO · crear
backlogs, no programar · curado manda (§5).

## 1 · Estado

- **R2 cerrada 6/6** — disciplina íntegra (una nota, firma, NEXT, DRAFT con
  `BLOQUEA:`; V corrigió su propio asiento contra el informe: ejemplar).
- **Espejo GitHub 6/6** ✅ — ramas y tips verificados de facto:
  S `6b2ba25` (sello `37c675a`) · O `9b94422` (`o_sdk-vigilancia` nueva;
  la vieja `o_sdk` es un rescate ajeno y queda intacta) · V `7ad07ea` ·
  Z `e21c0dd` · G `a0dfa3c` · L `76a1165`. Gate §10.5: **cumplido**.
- **Grafo prueba-1: 0/7 marcas** — todas las entradas esperan tick.
- **Temis** (auditor §11) operativo: R2 fue simulación; desde R3 cura en la
  pausa del tick, empezando por **piloto G**.
- Cherry-pick `BLOQUEA:`: convergencia de los 6 en el **holón-7**; después
  segunda puerta (V20) · kit 4·3·2 + skillización (L) · resto a F2.

## 2 · Asientos nuevos del custodio (GO — se ejecutan, no se discuten)

a) **Política de nodo ratificada** (✎ auditoría, orden del custodio):
   **apertura anónima base + peercard opt-in**. El nodo no convierte la
   card en requisito. Vale para TODAS las entradas al grafo; el CA de
   WP-O01 ya distingue ambas modalidades.
b) **REFACTOR (O↔V): decidido.** V lo emite en su próximo tick → propaga a
   O → **se zanja en el turno siguiente**. Hasta entonces nadie mueve
   claves, puertos ni contrato.
c) **Watchers PARADOS hasta nueva orden** (vigilancia y timbre). El
   custodio hace de timbre; el estado se refresca **solo** desde el
   handoff/informe del orquestador en cada tick. Causa técnica confirmada:
   R-1 (abajo).
d) **Compactar y reemplazar** (§3): vigente para todos. Notas y DRAFTs se
   sustituyen, no se acumulan; la historia queda en los sellos. Levantar
   estado barato = sesión más larga.

## 3 · Resuelto en R2 (no re-preguntar)

| qué | resolución |
| --- | ---------- |
| Vía de peercard para entrar al grafo | §2.a — anónimo base, card opt-in |
| Nombre de rama en CUADERNOS | patrón `RAMA(X)` = `<mundo>-vigilancia` (familia `_`); precedentes V/O |
| R-1 (O): regla 15 del watcher inunda `anomalias.log` (falso positivo del espejo de skills; afecta a todo carril con `skills:sync`) | registrado · watchers ya parados · el fix es de método → va al porte del skill (L, al cierre) |
| Congelar vs refactor | ver §2.b — decidido REFACTOR, en propagación |

## 4 · Lo que viene (ticks del custodio; no anticiparse)

★ **Z-runtime** (peercard en vivo + ficha de los 3 servicios del grafo) —
prerequisito de marcas honestas → **entradas al grafo** (V→G, O→Z, S
barrio, L, custodio) → hilo **peercard-reúso** (Z·G, compactador por
nombrar) · cruce **segunda puerta** V↔Z(+O) · L (kit 4·3·2 ◆) · S (reparar
timbre hub + cherry-pick `WP-S-R2-01`).

Cada carril: espera tu tick. Dudas nuevas → tu próxima nota, no antes.

— **Anfitrión**
