# BRIEF · PD-02 — spike peercard (submanual + decisión skill admin)

| campo | valor |
| ----- | ----- |
| WP | PD-02 (spike) |
| tipo / eje | contrato/capa documental · **Eje IV** (dos clientes: operador H y operador M) |
| `ALCANCE_DIFF` | `playground/prueba-de-dos/reference/PEERCARD.md` |

## CA

1. Submanual aparte (`reference/PEERCARD.md`) con las **tres vías** de
   identidad del peer, documentadas con fuentes del stack (no inventadas):
   - **ya tiene** card → verificación de vigencia (`peerCardPhase` /
     `peerCardRemainingMs` de `@zeus/protocol`);
   - **quiere una** → instrucciones para el **operador-admin** del nodo
     rooms: emisión automática al join (`onPeerCard`) y explícita
     (`issuePeerCard`) de `@zeus/authority-kit`, guardarraíles (no
     escalar scopes; ACL direccional; TTL);
   - **anónimo** → handshake `{ token, room, user }` de `@zeus/rooms`,
     límites (sin WebRTC; ACL default deny).
2. Los handoffs H y M apuntan al submanual antes de entrar; la vía
   elegida es parte del Registro (CA de la prueba).
3. Queda **encolada al custodio** la decisión: crear o no un skill
   `operador-rooms` para el operador-admin del nodo (emisión, ACL,
   salud). El spike NO lo implementa.

## Fuera de alcance

Implementar emisión, tocar `@zeus/*`, o crear el skill del admin.
